package Vista;

import Patter.Master;

public class Principal {

	public static void main(String[] args) {
		Master m = new Master("simon bonilla mesa camacho");
		m.run();
		// TODO Auto-generated method stub
		
		// Sentence palabra = new Sentence("simon bonilla mesa camacho");
		
		// sacarCola elementoCola = new sacarCola(palabra);
		
		// FijarTexto fijador = new FijarTexto(elementoCola);
		
		// RotarPalabra rotador = new RotarPalabra(elementoCola,fijador);
		// rotador.aplicarRotacion(palabra.cantidadPalabras());
		
		// System.out.println("palabra rotada: "+ palabra.getTexto());
		//Sort o = new Sort(p);
		
		//System.out.println("ordenado : " + o.quicksort());
	}

}